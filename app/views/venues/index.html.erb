
<!-- http://www.caever.com.au/ -->

<div class="wrapper">
  <div class="row">
    <div class="col-xs-6 col-md-4 ">
      <div class="stream-entry-inner" >
        <div class="stream-image" style="background-image: linear-gradient(rgba(0,0,0,0), rgba(0,0,0,0)), url('<%= image_path "bar.png" %>');">
        </div>
        <div class="border-bc">
          <div class="stream-body">
            <span>Some Text</span>
          </div>
          <div class="stream-body">
            <a href="#"><i class="fa fa-instagram"></i></a>
            <span>Some Text</span>
            <a id="heart" href="#"><i class="fa fa-heart-o"></i></a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>














<div class="col-md-4">

  <div id="map" style="width: 100%; height: 100vh;"></div>

  <% content_for(:after_js) do %>
  <script>
    $(document).ready(function() {
      var handler = Gmaps.build('Google');
      handler.buildMap({ internal: { id: 'map' } }, function() {
        markers = handler.addMarkers(<%= raw @hash.to_json %>);
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        if (markers.length == 0) {
          handler.getMap().setZoom(2);
        } else if (markers.length == 1) {
          handler.getMap().setZoom(14);
        }
      });
    });
  </script>
  <% end %>
</div>

